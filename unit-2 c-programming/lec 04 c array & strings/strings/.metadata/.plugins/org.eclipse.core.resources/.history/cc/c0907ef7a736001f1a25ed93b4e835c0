
#include <stdio.h>
#include <stdlib.h>

int main(void) {
	int strlen;
	char s[50];
	printf("Enter a string: ");
	fflush(stdin);fflush(stdout);
	gets(s);
	int i=0;
	while(s[i])!=0){

		i++;
	}
	return EXIT_SUCCESS;
}
