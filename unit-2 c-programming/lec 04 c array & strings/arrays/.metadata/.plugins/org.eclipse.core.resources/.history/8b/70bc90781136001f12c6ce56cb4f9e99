

#include <stdio.h>
#include <stdlib.h>

int main(void) {
	const int n;
	printf("Enter the numbers of data: ");
	fflush(stdin);fflush(stdout);
	scanf("%d",&n);
	float nums[n];

	return EXIT_SUCCESS;
}
